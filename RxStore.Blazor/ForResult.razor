@namespace RxStore

@using Microsoft.FSharp.Core


@typeparam TOk
@typeparam TError


@if (Of.IsOk)
{
    if (WhenOk != null)
    {
        @WhenOk(Of.ResultValue)
    }
}
else if (Of.IsError)
{
    if (WhenError != null)
    {
        @WhenError(Of.ErrorValue)
    }
}


@code
{
    [Parameter]
    public FSharpResult<TOk, TError> Of { get; set; }

    [Parameter]
    public RenderFragment<TOk> WhenOk { get; set; }

    [Parameter]
    public RenderFragment<TError> WhenError { get; set; }
}
