@namespace RxStore

@inject IEnumerable<IDevToolsConnection> AllDevToolsConnections

@code
{
    protected override void OnInitialized()
    {
        foreach (var devToolsConnection in AllDevToolsConnections)
        {
            devToolsConnection.Start();
        }
    }
}
