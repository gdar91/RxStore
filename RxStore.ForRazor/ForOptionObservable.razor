@namespace RxStoreC

@using Microsoft.FSharp.Core


@typeparam T


<RxStore.Subscribe To="Of" Context="option">
    <ForOption Of="option">
        <WhenSome Context="value">
            @if (WhenSome != null)
            {
                @WhenSome(value)
            }
        </WhenSome>
        <WhenNone>
            @if (WhenNone != null)
            {
                @WhenNone
            }
        </WhenNone>
    </ForOption>
</RxStore.Subscribe>


@code
{
    [Parameter]
    public IObservable<FSharpOption<T>> Of { get; set; }

    [Parameter]
    public RenderFragment<T> WhenSome { get; set; }

    [Parameter]
    public RenderFragment WhenNone { get; set; }
}
